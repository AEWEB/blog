<%= form_for(@task_history, :html => {onsubmit: "add_history('#{@task.id}','#{form_authenticity_token()}}'); return false;".html_safe}) do |f| %>
  <% if @task_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_history.errors.count, "error") %> prohibited this task_history from being saved:</h2>

      <ul>
      <% @task_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :memo %><br>
    <%= f.text_area :memo %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.time_select :start_time, :default => Time.now  %>
  </div>
  
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.time_select :end_time, :default => Time.now  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
